import { Search } from 'lucide-react'

const SearchBar = () => {
	return (
		<div>
			<Search className="h-5 w-5 hover:text-darkColor hoverEffect cursor-pointer" />
		</div>
	)
}

export default SearchBar
